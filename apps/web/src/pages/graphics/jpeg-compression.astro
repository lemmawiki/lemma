---
// Legacy unprefixed URL — content lives in
// src/content/pages/applications/jpeg-compression/{en,ko}.mdx, served at
// /en/graphics/jpeg-compression/ and /ko/graphics/jpeg-compression/. We keep
// this file as a redirect stub (rather than 404) for any external bookmark
// or internal link that points at the unprefixed URL. Astro emits static
// redirect HTML with meta refresh + canonical + robots:noindex.
return Astro.redirect("/en/graphics/jpeg-compression/", 301);
---
