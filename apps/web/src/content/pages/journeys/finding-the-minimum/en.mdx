---
title: Finding the Minimum · Lemma
description: One skeleton — objective, move, step, stop — under three applications across ML and finance.
breadcrumb:
  current: journey · finding the minimum
---

import { JourneyDays } from "@/components/mdx";
import Hook from "@/components/page/hook.astro";
import Lede from "@/components/page/lede.astro";
import PageFooter from "@/components/page/page-footer.astro";

<Hook kicker="journey · 4 days · ml → finance">

# Finding the Minimum

The nouns change — *loss*, *calibration error*, *portfolio risk*. The shape is the same — choose a number to improve, move through choices, stop when improvement runs out. This path reads the optimization module first, then walks the three applications that already use it, with the same five-step skeleton in mind on every page.

<Lede>
  Most of math that calls itself *fitting*, *tuning*, *minimizing*, or *finding equilibrium* is one
  procedure under different vocabularies. Read the skeleton once; the rest of the path is the
  same five steps with three different *what to improve*.
</Lede>

</Hook>

<JourneyDays id="finding-the-minimum" />

<PageFooter>
  Path through: <strong>ml</strong> and <strong>finance</strong> pillars.
  Modules consumed: <code>optimization</code>.
  Applications passed: <code>gradient-descent</code>, <code>model-calibration</code>, <code>portfolio-risk</code>.
  Destination — *the same skeleton runs all three; the nouns at each step are what differs.*
</PageFooter>
