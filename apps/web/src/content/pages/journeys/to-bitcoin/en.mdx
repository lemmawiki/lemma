---
title: To Bitcoin in 14 days · Lemma
description: Two applications, three modules, one trip. The most expensive pizza, ending at the signature scheme that keeps a wallet safe.
breadcrumb:
  parent: home
  parent_path: /en
  current: journey · to bitcoin
---

import { JourneyDays } from "@/components/mdx";
import Hook from "@/components/page/hook.astro";
import Lede from "@/components/page/lede.astro";
import PageFooter from "@/components/page/page-footer.astro";

<Hook kicker="journey · 14 days · finance">

# To Bitcoin in 14 days

Two applications, three modules, one trip. Open with the most expensive pizza in history; close knowing why a wallet stays safe.

<Lede>
  Lemma is a graph, not a sequence. This page is _one_ author's pick of one path through it, with a destination — the [Bitcoin Signature](/en/finance/bitcoin-signature) — in mind. Read the days in order, or jump anywhere; progress is tracked locally and you can reset it whenever.
</Lede>

</Hook>

<JourneyDays id="to-bitcoin" />

<PageFooter>
  Path through: <strong>finance</strong> pillar.
  Modules consumed: <code>log</code>, <code>vectors</code>, <code>bezout</code>.
  Applications passed: <code>bitcoin-pizza</code>, <code>curve-intersections</code>, <code>bitcoin-signature</code>.
</PageFooter>
