---
title: Change and accumulation in 7 days · Lemma
description: How calculus runs both physics and finance — same math, different nouns.
breadcrumb:
  current: journey · change and accumulation
---

import { JourneyDays } from "@/components/mdx";
import Hook from "@/components/page/hook.astro";
import Lede from "@/components/page/lede.astro";
import PageFooter from "@/components/page/page-footer.astro";

<Hook kicker="journey · 7 days · physics → finance">

# Change and accumulation in 7 days

Calculus is two operations — *change* and its *accumulation* — and the same pair runs both physics and finance. Distance and present value are the same shape, with different rates plugged in. This path walks the pair across both pillars, in order, with a deliberate stop in the middle to admit they really are the same machine.

<Lede>
  Lemma is a graph, not a sequence. This page is *one* author's pick of one cross-pillar path through it: physics on days 1–5, finance on day 6, and a final review that lets the reader see, in one frame, the same accumulator at work in distance and present value.
</Lede>

</Hook>

<JourneyDays id="change-and-accumulation" />

<PageFooter>
  Path through: <strong>physics</strong> and <strong>finance</strong> pillars.
  Modules consumed: <code>derivatives</code>, <code>integration</code>.
  Applications passed: <code>projectile-motion</code>, <code>terminal-velocity</code>, <code>present-value</code>.
  Destination — *same accumulator, different nouns: distance in physics, present value in finance.*
</PageFooter>
