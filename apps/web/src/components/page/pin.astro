---
// "Pin" — the bordered-blue summary block that closes a page (the manifesto's
// "what stays after you forget the prose"). Sat at the bottom of an arc.
---
<div
  class="mt-[22px] rounded-r-md border-l-4 border-acc bg-acc-soft px-[22px] py-[18px] text-base leading-[1.55] text-acc-deep [&_strong]:font-semibold [&_strong]:text-acc-deep [&_em]:italic"
>
  <slot />
</div>
