---
import { History as HistoryReact } from "../widgets/history";

interface Props {
  concept: string;
}
const { concept } = Astro.props;
const lang: "en" | "ko" = (Astro.currentLocale as "en" | "ko" | undefined) ?? "en";
---

<HistoryReact concept={concept} language={lang} client:load />
